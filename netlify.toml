[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false

# SPA fallback — client-side routing (react-router) handles all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
