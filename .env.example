# Copy to .env and fill in. Vite exposes VITE_* variables to the client.

# Mapbox token for the map module (free at https://account.mapbox.com)
# Without it, map components render a setup card instead of a map.
VITE_MAPBOX_TOKEN=
